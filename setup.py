from setuptools import setup, find_packages

setup(
    name='metaforge',
    version='0.1.0',
    packages=find_packages(),
    install_requires=['numpy'],
    author='Mageed Ghaleb',
    description='Metaheuristic-powered production scheduling in Python.',
)
